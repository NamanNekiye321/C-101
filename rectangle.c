#include <stdio.h>

int main() {
    
    double width = 10.2;
    double height = 11.2;
    double perimeter = 0 ;
    double area = 0 ;

    perimeter = 2*(width+height);
    area = width*height;  

    printf("The perimeter of given rectangle given its width and height is %lf and %lf respectively is   %lf and the area of given rectangle is %lf",width , height, perimeter , area);



    








    return 0;
}